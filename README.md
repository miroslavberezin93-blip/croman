# croman

Convert integers (1–3999) to Roman numerals in Linux.

## Install

```bash
curl -s https://raw.githubusercontent.com/miroslavberezin93-blip/croman/main/install.sh | bash
```
After that, open a new terminal (or run source ~/.bashrc) and use

## Usage
```
croman <integer>
croman --help
croman --version
```
---